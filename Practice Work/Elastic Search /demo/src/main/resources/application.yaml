spring:
  application:
    name: demo
  datasource:
    url: jdbc:postgresql://localhost:5432/test_db
    username: postgress
    password: Admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  elasticsearch:
    uris:
    - http://localhost:9200
server:
  port: 8080
